import api from './api';
/*
// Admin Menu Management APIs
export const createMenuItem = (data) => api.post('/api/menu/items', data);
export const updateMenuItem = (id, data) => api.put(`/api/menu/items/${id}`, data);
export const deleteMenuItem = (id) => api.delete(`/api/menu/items/${id}`);
export const getAllMenuItems = () => api.get('/api/menu/items');
export const getPriceHistory = (id) => api.get(`/api/menu/items/${id}/price-history`);
export const createWeeklyMenu = (data) => api.post('/api/menu/weekly', data);

// Employee Menu View APIs
export const getCurrentWeeklyMenu = () => api.get('/api/menu/weekly/current');
export const getTodaysMenu = (day) => api.get(`/api/menu/daily/${day}`);

// Order Management APIs
export const getAllOrders = () => api.get('/api/orders');
export const updateOrderStatus = (id, status) => api.put(`/api/orders/${id}/status`, { status });
export const filterOrders = (params) => api.get('/api/orders/filter', { params });
*/